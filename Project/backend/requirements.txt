# SE-GUARD ML Backend — Requirements
# Install with:  pip install -r requirements.txt

# ── Web Framework ──
flask==3.0.3
flask-cors==4.0.1
gunicorn

# ── ML / Data Science (all pip-installable) ──
scikit-learn==1.3.2
numpy==1.24.4
pandas==2.2.2
joblib==1.4.2

# ── NLP (lightweight, no extra downloads needed) ──
nltk==3.8.1

# ── Security ──
bcrypt==4.1.3
PyJWT==2.8.0

# ── Database ──
pymongo==4.7.3

# ── Utilities ──
python-dotenv==1.0.1
